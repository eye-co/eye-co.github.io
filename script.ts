// // script code here
// const aiko: number = 5;
// const box = document.getElementById("clusterCanvas");
// const ctx = box.getContext("2d");
// box.width = 500;
// box.height = 500;

// ctx.moveTo(0, 0);
// ctx.lineTo(200, 100);
// ctx.stroke();

// interface Point {
//     x: number;
//     y: number;
// }

// const myPoints: Point[] = [];

// function randomPointGenerator(ct: number): void {
//     for (let i = 0; i < ct; i++) {
//         myPoints.push({x: Math.random() * box.width, y : Math.random() * box.height})
//     }
// }

// function drawPoints(points: Point[]): void {
//     myPoints.forEach(point => {
//         ctx?.beginPath();
//         ctx?.arc(point.x, point.y, 50, 0, Math.PI * 2); // x, y, radius, start angle, end angle
//         ctx!.fillStyle = 'black';
//         ctx?.fill();
//     });
// }

// randomPointGenerator(100)
// drawPoints(myPoints)
